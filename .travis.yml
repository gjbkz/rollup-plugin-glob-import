language: node_js
cache: npm
os: osx
node_js: '12'
install:
  - npm ci
  - npm install -g codecov c8 nyc
script:
  - c8 npm test
  - c8 report --reporter=text-lcov > coverage.lcov
  - codecov
  - c8 report --reporter=text-lcov > coverage.lcov
  - codecov
  - nvm install 10
  - npm test
  - nvm install 8
  - nyc npm test
  - codecov
